import { TransactionHistory } from '@/features/point-transactions/components/transaction-history';

export const metadata = {
  title: 'History — Beer House',
};

export default function HistoryPage() {
  return <TransactionHistory />;
}
