# Backend API base URL (no trailing slash, no /api/v1 — that is appended by the API client)
VITE_API_URL=http://localhost:8080

# Admin app public URL (used for absolute links / OAuth callbacks if needed)
VITE_APP_URL=http://localhost:3001
